# Melody Backoffice - Environment variables template
# Copy this file to .env.local and fill in the values

# Environment Mode
# Options: "development" (uses localhost microservices) or "production" (uses API Gateway)
NODE_ENV=development

# API Gateway URL (required for production mode)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Microservices URLs (used when NEXT_PUBLIC_ENV=development)
# API Configuration (localhost)
USER_SERVICE=http://localhost:8080 # (prod)
CATALOG_SERVICE=http://localhost:30003 # (dev)

# Firebase Storage
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-firebase-storage-bucket